---
title: 机器学习(5)无监督学习(Unsupervised Learning)及异常检测(Outlier Detection)
date: 2019-12-20T00:00:00+08:00
categories:
  - 笔记
tags:
  - 机器学习
---
> 本文为吴恩达《机器学习》课程学习笔记的第5篇；
> 视频/作业/PPT等资源可在[Coursera](https://zh.coursera.org/learn/machine-learning)和[网易云课堂](https://study.163.com/course/courseLearn.htm?courseId=1004570029#/learn/video?lessonId=1049052745&courseId=1004570029)等处找到；

# 无监督学习

## K-means聚类

首先选择K个随机的点，为**聚类中心**，然后对于数据集中的每一个数据，按照距离K个中心点的距离，将其与距离最近的中心点归为一类。再计算每个类的平均值，把该类的中心点移动到平均值的位置，重复对数据点进行聚类。——直到中心点不再变化。

### 随机初始化

随机选择K<m（训练集实例数量）个训练实例，然后令K个聚类中心分别与这K个训练实例相等。

为了解决局部最小值问题，通常进行多次K-means运算，每一次都重新进行随机初始化，最后再比较结果。

### 选择聚类数（肘部法则）

更改K的值，直到代价函数下降较慢。（类似PCA碎石图）

## 数据降维

从二维降到一维 → 如将二元坐标点投影到一条线段上。

从三维到二维 → 如将三元坐标点投影到一个二维平面上。

数据可视化？ → 新产生特征的意义需要自己去发现。

### 主成分分析（PCA）

第一步，均值归一化，计算出所有特征的均值，然后令$x_j=x_j-\mu_j$，如果特征在不同数量级上，还需要除以标准差$\sigma^2$。

第二步，计算协方差矩阵：

$$
\Sigma=\cfrac{1}{m}\sum_{i=1}^n(x^{(i)})(x^{(i)})^T
$$

第三步，计算协方差矩阵的特征向量：

$$
[U,S,V] = svd(\Sigma)
$$

U即是一个具有与数据之间最小投射误差的方向向量构成的矩阵，若想将数据从n维降到k维，只需从U中选取前k个向量，得到一个新的n×k的矩阵用$U_reduce$表示，然后计算得新的特征向量：

$$
z^{(i)}=U_{reduce}^T*x^{(i)}
$$

x是n×1维的，所以结果为k×1维。

而S是一个n×n的矩阵，只有对角线上有值，可以用来计算平均均方误差与训练集方差的比例（小于1%为例）：

$$
\cfrac{\sum_{i=1}^{k}S_{ii}}{\sum_{i=1}^{n}S_{ii}}>=0.99
$$

压缩数据后，近似地获得原有特征：

$$
x^{(i)}_{approx}=U_{reduce}^T*z^{(i)}
$$

# 异常检测

给定数据集，已知数据集正常，那么如何判断一个新的数据是不是异常值，以及这个新数据不属于这一组数据的几率如何？

## 高斯分布

高斯分布即正态分布，符合之的变量$x~N(\mu,\sigma^2)$,其概率密度函数为：

$$
p(x,\mu,\sigma^2)=\cfrac{1}{\sqrt{2\pi}\sigma}exp{-\cfrac{(x-\mu)^2}{2\sigma^2}}
$$

其中的$\mu$和$\sigma^2$分别为均值和方差，$\mu$影响中点位置，$\sigma^2$影响峰的宽度高度。

## 异常检测算法

给定数据集$x^{(1)}-x^{(m)}-$，则针对每一个特征计算mu和sigma的估计值：

$$
\mu_j=\cfrac{1}{m}\sum_{i=1}^{m}x_j^{(i)}
$$

$$
\sigma_j^2=\cfrac{1}{m}\sum_{i=1}^{m}(x_j^(i)-\mu_j)^2
$$

获得两个估计值后，和给定的新训练实例$x_j$一起代入高斯分布计算p(x)：

$$
p(x)=\prod^{n}_{j=1}p(x_j;\mu_j;\sigma_j^2)
$$

当$p(x)<\epsilon$时即为**异常**。

注意：异常检测与监督学习有些相似，但最大的不同在于异常检测的正向类（异常数据y=1）非常少，负向类非常多（正常数据y=0）！而监督学习同时拥有大量的正和负类。

## 开发异常检测系统

作为一个非监督学习算法，在用其开发一个异常检测系统时，需要注意数据的选取：

如有10000个正常数据，其中有20个异常，那么建议如下**分配**：

6000个正常数据作为训练集；

2000个正常数据和10个异常数据作为交叉检验集；

2000个正常数据和10个异常数据作为测试集。

而**评价方法**如下：

1.根据训练集数据，估计特征的均值和方差，并构建p(x)；

2.对交叉检验集，用不同的$\epsilon$值作阈值，预测数据是否异常；并根据F1选择$\epsilon$；

3.选出$\epsilon$后，针对测试集进行预测，计算F1值。

## 选择特征

如果数据分布不是高斯分布，可以通过对数变换或者指数变换使之成为高斯分布。

---

**© 版权声明**：本文为 [Zhe Li](https://lizh3.github.io/) 原创，依据 [CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/) 许可证进行授权，转载请附上出处链接及本声明。